sudo: required
services:
    - docker

before_install:
    - docker build -t lipewinning/react-test -f./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true lipewinning/react-test npm test -- --coverage

after_success:
    - docker build -t lipewinning/multi-client ./client
    - docker build -t lipewinning/multi-nginx ./nginx
    - docker build -t lipewinning/multi-server ./server
    - docker build -t lipewinning/multi-worker ./worker
    #LOG IN DOCKER CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    #- docker login -e="$DOCKER_EMAIL" -u="$DOCKER_ID" -p="$DOCKER_PASSWORD";
    #push imagens to dockerhub
    - docker push lipewinning/multi-client
    - docker push lipewinning/multi-nginx
    - docker push lipewinning/multi-server
    - docker push lipewinning/multi-worker